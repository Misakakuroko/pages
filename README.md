# pages

Encrypted static pages (AES-256-GCM, PBKDF2-SHA256 600k iterations).
All content is ciphertext; decryption happens client-side in the browser with a password.
Organized by sub-directory per project.
